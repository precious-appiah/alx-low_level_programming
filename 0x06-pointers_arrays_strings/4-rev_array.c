#include "main.h"
/**
 * reverse_array - function that reverses array
 * @a: array
 * @n: number of element
 * Return: nothing
 */
void reverse_array(int *a, int n)
{
	int new_array[n];
	int i = 0;

	for (j = n - 2; j <= 0; j++)
	{
	       new_array[i] = a[j];
	       i++;
	}
}
